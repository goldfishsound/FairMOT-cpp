### test/CMakeLists.txt ###

cmake_minimum_required(VERSION 3.5 FATAL_ERROR)

# set(GTEST_PATH "/opt/homebrew/Cellar/googletest/1.17.0/lib/cmake/GTest")
find_package(GTest REQUIRED)

add_executable(my_test my_test.cpp)

set(INSTALL_DIR "/Users/thomas/Developer/projects/FairMot-cpp/product/FairMotPackage")
target_include_directories(my_test
    PRIVATE
        "${INSTALL_DIR}/include"
)


# Apple Silicon specific flags
if(APPLE)
    find_library(ACCELERATE_FRAMEWORK
    NAMES Accelerate
    PATHS ${CMAKE_SYSTEM_FRAMEWORK_PATH}
    PATH_SUFFIXES Frameworks
)
    # Check if the architecture is arm64 (Apple Silicon)
    if(CMAKE_SYSTEM_PROCESSOR STREQUAL "arm64")
        set(CMAKE_OSX_ARCHITECTURES "arm64")
    else()
        set(CMAKE_OSX_ARCHITECTURES "x86_64")
    endif()
endif()

# Find all static and dynamic libraries under the dependencies directory
file(GLOB_RECURSE DEP_LIBS
    "${INSTALL_DIR}/lib/dependencies/*.a"
    "${INSTALL_DIR}/lib/dependencies/*.dylib"
)

foreach(lib ${DEP_LIBS})
    message(STATUS "  ${lib}")
endforeach()

target_link_libraries(my_test 
    ${DEP_LIBS}
    ${ACCELERATE_FRAMEWORK}
    GTest::gtest 
    GTest::gtest_main
    ${INSTALL_DIR}/lib/libawtracker.a)


# Set rpath so dynamic libraries under dependencies are found at runtime
set_target_properties(my_test PROPERTIES
    BUILD_RPATH "${INSTALL_DIR}/lib/dependencies"
    INSTALL_RPATH "${INSTALL_DIR}/lib/dependencies"
)

add_test(NAME MyTest COMMAND my_test)